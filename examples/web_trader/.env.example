WEB_PUBLISH_PORT=8000
WEB_USERNAME=vnpy
WEB_PASSWORD=vnpy
WEB_SECRET_KEY=change-me-to-a-long-random-string

DATABASE_USER=admin
DATABASE_PASSWORD=quest
DATABASE_NAME=qdb

# 国内机器拉不到 Docker Hub 时改用镜像，例如：
# QUESTDB_IMAGE=docker.m.daocloud.io/questdb/questdb:8.2.3
QUESTDB_IMAGE=questdb/questdb:8.2.3
DEBIAN_MIRROR=

# 实盘铁鹰（生产设 LIVE_IRON_CONDOR=1 且 LIVE_DRY_RUN=0）
LIVE_IRON_CONDOR=0
LIVE_DRY_RUN=1
LIVE_PORTFOLIOS=IO.CFFEX
LIVE_GATEWAY=CTP
LIVE_SCRIPT=gex_tv_strangle.py

# IF 5 分钟高频均值回归（脚本页启动 if_hf_mr.py；默认模拟）
# HF_LOOK=16
# HF_Z_ENTRY=2.4
# HF_HOLD_BARS=6
# HF_STOP_ATR=1.5
# HF_TP_ATR=1.0
# HF_RISK=0.006
# HF_MAX_LOTS=4
# HF_VOL_MIN=0.12
# HF_SESSION_START=09:45
# HF_SESSION_END=11:25
